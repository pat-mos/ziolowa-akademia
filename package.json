{
  "name": "ziolowa-akademia",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=ziolowa-akademia"
  },
  "dependencies": {
    "@fontsource-variable/parkinsans": "^5.3.0",
    "@fontsource/quicksand": "^5.3.0",
    "astro": "^5.6.0",
    "lenis": "^1.3.25",
    "sharp": "^0.33.5"
  }
}
