---
import { Image } from 'astro:assets';
import oMnieFoto from '../assets/img/o-mnie.jpeg';

// Sekcja łączona: wprowadzenie o Ziołowej Akademii + cztery wartości.
// Układ 1:1 z sekcją „Gentle by design. Curious by nature." (bobolobo):
// u góry wyśrodkowany blok 680px (mała etykieta + duży tekst), pod nim
// siatka czterech kafelków z ikoną, tytułem i opisem.
const wartosci = [
  {
    ikona: 'lisc',
    tytul: 'Przez doświadczenie',
    opis: 'Zajęcia oparte na bezpośrednim kontakcie z\u00A0naturą i\u00A0pracy własnymi rękami.',
  },
  {
    ikona: 'oko',
    tytul: 'Uważność i\u00A0ciekawość',
    opis: 'Ćwiczymy koncentrację, obserwację i\u00A0cierpliwe przyglądanie się przyrodzie.',
  },
  {
    ikona: 'ludzie',
    tytul: 'Dla dzieci i\u00A0dorosłych',
    opis: 'Warsztaty dla przedszkoli i\u00A0szkół oraz spokojne spotkania dla dorosłych.',
  },
  {
    ikona: 'slonce',
    tytul: 'Blisko natury',
    opis: 'Spacery botaniczne i\u00A0kąpiele leśne, w\u00A0których natura jest miejscem zajęć.',
  },
];
---
<section class="section wartosci" id="o-mnie">
  <div class="container">
    {/* Wprowadzenie — wyśrodkowany blok jak `wrap-about-home` */}
    <div class="w-intro">
      <span class="w-label">O Ziołowej Akademii</span>
      <h2 class="w-headline">
        Natury nie da się nauczyć z książki. Ziołowa Akademia to warsztaty,
        spacery botaniczne i&nbsp;kąpiele leśne, w&nbsp;których dzieci i&nbsp;dorośli
        poznają rośliny wszystkimi zmysłami. Tam, gdzie zabawa staje się nauką,
        a&nbsp;nauka zabawą.
      </h2>
      <button type="button" class="btn btn-lime w-cta" data-o-mnie-open>
        O mnie
      </button>
    </div>

    {/* Cztery kafelki — jak `features-grid` */}
    <div class="w-grid">
      {wartosci.map((w) => (
        <article class="w-karta">
          <span class="w-ikona" aria-hidden="true">
            {w.ikona === 'lisc' && (
              <svg viewBox="0 0 64 64" fill="currentColor">
                <path d="M30.6,8.3c8.4,4.9,14.6,12.2,17.1,21.7c2.6,9.9-0.4,18.6-8.1,25.6c-3.6,3.3-8,4.2-12.5,2.4c-6.9-2.7-11.4-8-13.4-15.1c-3.2-11.4,0.4-21.2,8.7-29.3c2.2-2.2,5-3.9,8.2-5.3z M30.2,12.4c-2.4,1.2-4.4,2.6-6.1,4.3c-7.3,7.1-10.4,15.6-7.6,25.6c1.7,6,5.4,10.3,11.1,12.6c3.2,1.3,6.1,0.7,8.7-1.7c6.7-6.1,9.2-13.4,7-22c-2.2-8.3-7.5-14.7-14.8-19.1z" />
                <path d="M29.8,14.2c1.7,14.1,2.9,28.2,3.5,42.4c0.1,2.2-3.3,2.3-3.4,0.1c-0.6-14.1-1.8-28.1-3.4-42.1C26.2,12.4,29.6,12,29.8,14.2z" />
                <path d="M31,26.2c3.4-2.3,7-4.1,10.8-5.5c2.1-0.8,3.2,2.4,1.1,3.2c-3.5,1.3-6.9,3-10.1,5.1C31,30.2,29.2,27.4,31,26.2z" />
                <path d="M29.4,28.9c-2.6-2.4-5.4-4.5-8.5-6.2c-1.9-1.1-0.2-4,1.7-2.9c3.3,1.9,6.4,4.1,9.2,6.7C33.4,28,31,30.4,29.4,28.9z" />
                <path d="M31.9,38.4c3-1.9,6.2-3.5,9.5-4.7c2.1-0.8,3.2,2.4,1.1,3.2c-3.1,1.1-6,2.6-8.8,4.4C31.9,42.5,30,39.6,31.9,38.4z" />
                <path d="M30.5,42.6c-2.2-2-4.6-3.7-7.2-5.2c-1.9-1.1-0.2-4,1.7-2.9c2.8,1.6,5.4,3.5,7.8,5.6C34.4,41.7,32.1,44,30.5,42.6z" />
              </svg>
            )}
            {w.ikona === 'oko' && (
              <svg viewBox="0 0 64 64" fill="currentColor">
                <path d="M3.9,33.4C11.5,21.8,21.6,15.7,34,15.9c11.9,0.2,21.6,6.4,28.6,18c0.6,1,0.5,2.1-0.2,3c-7.9,10.4-17.9,15.6-30,15.3C20.7,51.9,11,46,3.7,36.4C3,35.5,3.2,34.4,3.9,33.4z M8,34.9c6.6,8.2,15,12.6,25.2,12.9c10.3,0.3,18.7-3.9,25.5-12.6C52,25,43.7,19.6,33.7,19.4C23.9,19.2,15.2,24.4,8,34.9z" />
                <path d="M33.8,23.6c5.2,0.3,9.1,4.7,8.8,9.9c-0.3,5.2-4.8,9.1-10,8.7c-5-0.4-8.8-4.8-8.5-9.9C24.4,27.2,28.8,23.3,33.8,23.6z M33.6,27.1c-3.2-0.2-5.9,2.2-6.1,5.4c-0.2,3.2,2.2,5.9,5.4,6.1c3.2,0.2,6-2.2,6.2-5.4C39.3,30,36.8,27.3,33.6,27.1z" />
                <path d="M33.2,29.6c1.9,0.1,3.4,1.8,3.3,3.7c-0.1,1.9-1.8,3.4-3.7,3.2c-1.8-0.1-3.2-1.8-3.1-3.7C29.8,31,31.4,29.5,33.2,29.6z" />
              </svg>
            )}
            {w.ikona === 'ludzie' && (
              <svg viewBox="0 0 64 64" fill="currentColor">
                <path d="M24.4,10.2c6.4-0.5,11.9,4.5,12.3,11.1c0.4,6.5-4.7,12-11.2,12.3c-6.3,0.3-11.6-4.7-11.9-11.1C13.4,16,18.1,10.7,24.4,10.2z M24.7,13.7c-4.3,0.3-7.6,4-7.4,8.4c0.2,4.3,4,7.6,8.4,7.4c4.2-0.2,7.5-4,7.2-8.3C32.7,16.9,29,13.4,24.7,13.7z" />
                <path d="M7.6,53.5c-1-10.9,7.5-20.1,18.4-20.5c11.1-0.4,20.4,8,21,19.1c0.1,1.9-2.9,2.1-3.1,0.2c-0.7-9.2-8.5-16-17.7-15.6c-8.8,0.4-15.4,8-14.7,16.7C11.7,55.2,8.7,55.4,7.6,53.5z" />
                <path d="M45.4,15c4.8-0.4,9,3.4,9.3,8.2c0.3,4.8-3.5,9-8.3,9.2c-1.6,0.1-1.9-3.3-0.3-3.4c2.9-0.2,5.1-2.7,4.9-5.6c-0.2-2.9-2.7-5-5.6-4.8C43.9,18.7,43.8,15.1,45.4,15z" />
                <path d="M48.5,35.2c8.6,1.3,15,8.8,14.6,17.5c-0.1,1.5-2.1,1.9-2.8,0.6c-0.3-0.5-0.3-1-0.3-1.5c0.3-6.9-4.8-12.7-11.6-13.6C46.6,38,46.9,35,48.5,35.2z" />
              </svg>
            )}
            {w.ikona === 'slonce' && (
              <svg viewBox="0 0 64 64" fill="currentColor">
                <path d="M32.6,17.9c8.2,0.4,14.5,7.2,14.1,15.4c-0.4,8.1-7.4,14.4-15.6,13.9c-7.9-0.5-14-7.3-13.6-15.3C17.9,23.8,24.6,17.5,32.6,17.9z M32.4,21.4c-6.1-0.3-11.2,4.4-11.5,10.6c-0.3,6.1,4.4,11.2,10.5,11.6c6.3,0.4,11.6-4.4,11.9-10.6C43.6,26.8,38.7,21.7,32.4,21.4z" />
                <path d="M30.8,13.2c-0.5-3.1-0.7-6.2-0.5-9.3c0.1-1.9,2.9-1.8,2.9,0.1c0,3-0.1,6.1-0.4,9.1C32.7,14.9,31.1,15,30.8,13.2z" />
                <path d="M31,51.5c0.2-1.8,2.9-1.7,2.9,0.2c-0.2,3-0.3,6-0.2,9c0,1.9-2.7,2-2.9,0.1C30.6,57.7,30.7,54.6,31,51.5z" />
                <path d="M51.3,30.9c3.1-0.5,6.2-0.6,9.3-0.4c1.9,0.1,1.8,2.9-0.1,2.9c-3,0-6.1-0.1-9.1-0.5C49.6,32.8,49.5,31.2,51.3,30.9z" />
                <path d="M3.4,31c3-0.4,6-0.5,9-0.3c1.9,0.1,1.8,2.9-0.1,2.9c-2.9,0-5.8-0.1-8.7-0.4C1.7,32.9,1.6,31.3,3.4,31z" />
                <path d="M44.9,17.4c2-2.4,4.2-4.6,6.6-6.6c1.5-1.2,3.3,0.9,1.9,2.1c-2.3,1.9-4.4,4-6.3,6.3C46,20.5,43.9,18.9,44.9,17.4z" />
                <path d="M12.6,50.5c1.9-2.3,4-4.5,6.2-6.4c1.4-1.2,3.3,0.9,1.9,2.2c-2.1,1.8-4.1,3.8-5.9,6C13.7,53.6,11.6,51.9,12.6,50.5z" />
                <path d="M51.4,53.2c-2.4-2-4.6-4.2-6.6-6.6c-1.2-1.5,0.9-3.2,2.1-1.8c1.9,2.3,4,4.4,6.3,6.3C54.5,52.3,52.9,54.4,51.4,53.2z" />
                <path d="M12.9,13.4c-1.2-1.4,0.9-3.2,2.2-1.9c1.8,2.1,3.8,4.1,6,5.9c1.4,1.2-0.3,3.3-1.8,2.1C16.9,17.7,14.8,15.6,12.9,13.4z" />
              </svg>
            )}
          </span>
          <div class="w-tekst">
            <h3>{w.tytul}</h3>
            <p>{w.opis}</p>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

{/* Modal „O mnie” — treść z opisu PDF, bez powtórzeń z intro sekcji */}
<div class="om-modal" id="o-mnie-modal" aria-hidden="true" inert>
  <div class="om-modal-backdrop" data-om-close></div>
  <div
    class="om-modal-dialog"
    role="dialog"
    aria-modal="true"
    aria-labelledby="om-modal-title"
  >
    <button type="button" class="om-modal-close" data-om-close aria-label="Zamknij">
      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true">
        <path d="M18 6 6 18M6 6l12 12" />
      </svg>
    </button>
    <div class="om-modal-scroll">
      <div class="om-modal-content">
        <figure class="om-photo">
          <Image
            src={oMnieFoto}
            alt="Monika Wierus-Wieczorek w wieńcu z kwiatów przy bramie Ziołowy Zakątek"
            widths={[480, 720, 960]}
            sizes="(max-width: 720px) 92vw, 640px"
            loading="lazy"
          />
        </figure>

        <p class="om-eyebrow">Twórczyni</p>
        <h3 id="om-modal-title">Monika Wierus-Wieczorek</h3>
        <p class="om-subtitle">Biolog, pedagog i&nbsp;edukatorka przyrodnicza</p>

        <p class="om-lead">
          Nazywam się Monika i&nbsp;jestem twórczynią Ziołowej Akademii.
          Specjalizuję się w&nbsp;zajęciach edukacyjnych dla dzieci opartych
          na bezpośrednim kontakcie z&nbsp;naturą — poza salą lekcyjną,
          tam gdzie nauka staje się angażująca i&nbsp;bliska rzeczywistości.
        </p>

        <h4 class="om-h">Skąd biorę warsztaty</h4>
        <p class="om-p">
          Przez ponad 10&nbsp;lat pracowałam w&nbsp;zielarniach: poznawałam
          praktyczne zastosowanie roślin i&nbsp;ich rolę w&nbsp;codziennym życiu.
          Dziś łączę to doświadczenie z&nbsp;pracą pedagogiczną — tworzę
          autorskie warsztaty przyrodnicze, biologiczne i&nbsp;zielarskie
          dla przedszkoli i&nbsp;szkół podstawowych.
        </p>
        <p class="om-p">
          Prowadzę też zajęcia w&nbsp;placówkach oświatowych oraz spacery
          botaniczne i&nbsp;kąpiele leśne. W&nbsp;ofercie znajdziesz m.in.
          Akademię Małego Odkrywcy Ciała i&nbsp;warsztaty o&nbsp;roślinach,
          ziołach i&nbsp;dzikiej przyrodzie.
        </p>

        <h4 class="om-h">Co wspierają zajęcia</h4>
        <ul class="om-check">
          <li>treści przyrodnicze i&nbsp;biologiczne w&nbsp;praktyce</li>
          <li>rozwój kompetencji poznawczych</li>
          <li>naukę przez doświadczenie i&nbsp;eksperyment</li>
          <li>budowanie relacji z&nbsp;naturą</li>
          <li>pracę metodą aktywizującą</li>
        </ul>

        <h4 class="om-h">Dla przedszkoli i&nbsp;szkół</h4>
        <p class="om-p">
          Warsztaty sprawdzają się jako uzupełnienie programu nauczania,
          zajęcia tematyczne, wydarzenia specjalne albo element edukacji
          terenowej — tak, by placówka dostała konkretne doświadczenie,
          a&nbsp;nauczyciele oddech.
        </p>

        <h4 class="om-h">Jak to się zaczęło</h4>
        <p class="om-p">
          Ziołowa Akademia wyrosła z&nbsp;obserwacji dzieci i&nbsp;ich potrzeby
          odkrywania świata przez działanie. Chciałam stworzyć przestrzeń,
          w&nbsp;której wiedza biologiczna spotyka się z&nbsp;pedagogiką
          doświadczeń — z&nbsp;większą ilością natury, ruchu i&nbsp;realnej
          pracy własnymi rękami.
        </p>
      </div>
    </div>
    <div class="om-modal-foot">
      <a href="#kontakt" class="btn btn-lime" data-om-close>Zapytaj o termin</a>
    </div>
  </div>
</div>

<script>
  // Kafelki pojawiają się kolejno, gdy sekcja wjedzie w widok.
  const karty = document.querySelectorAll('.wartosci .w-karta');
  const io = new IntersectionObserver((wpisy) => {
    wpisy.forEach((w) => {
      if (!w.isIntersecting) return;
      const el = w.target as HTMLElement;
      el.style.transitionDelay = (el.dataset.i || '0') + 'ms';
      el.classList.add('in-view');
      io.unobserve(el);
    });
  }, { threshold: 0.25 });
  karty.forEach((c, i) => {
    (c as HTMLElement).dataset.i = String(i * 80);
    io.observe(c);
  });

  // --- Modal „O mnie” ---
  const modal = document.getElementById('o-mnie-modal');
  const openBtn = document.querySelector<HTMLElement>('[data-o-mnie-open]');
  if (modal && openBtn) {
    let trigger: HTMLElement | null = null;

    const focusable = () =>
      modal.querySelectorAll<HTMLElement>(
        'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])'
      );

    const open = () => {
      trigger = openBtn;
      modal.classList.add('open');
      modal.setAttribute('aria-hidden', 'false');
      modal.removeAttribute('inert');
      document.documentElement.classList.add('om-modal-lock');
      (window as any).lenis?.stop?.();
      modal.querySelector<HTMLElement>('.om-modal-close')?.focus();
    };

    const close = () => {
      modal.classList.remove('open');
      modal.setAttribute('aria-hidden', 'true');
      modal.setAttribute('inert', '');
      document.documentElement.classList.remove('om-modal-lock');
      (window as any).lenis?.start?.();
      trigger?.focus();
      trigger = null;
    };

    openBtn.addEventListener('click', open);
    modal.querySelectorAll('[data-om-close]').forEach((el) => {
      el.addEventListener('click', (e) => {
        const a = el as HTMLAnchorElement;
        if (a.tagName === 'A' && a.getAttribute('href')?.startsWith('#')) {
          // Zamknij, potem pozwól Layoutowi zescrollować do kotwicy
          close();
          return;
        }
        e.preventDefault();
        close();
      });
    });

    document.addEventListener('keydown', (e) => {
      if (!modal.classList.contains('open')) return;
      if (e.key === 'Escape') {
        e.preventDefault();
        close();
        return;
      }
      if (e.key !== 'Tab') return;
      const nodes = [...focusable()];
      if (!nodes.length) return;
      const first = nodes[0];
      const last = nodes[nodes.length - 1];
      if (e.shiftKey && document.activeElement === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && document.activeElement === last) {
        e.preventDefault();
        first.focus();
      }
    });
  }
</script>

<style is:global>
  html.om-modal-lock,
  html.om-modal-lock body {
    overflow: hidden;
  }
</style>

<style>
  /* Bez własnego tła — sekcja siedzi na białym tle strony.
     Odstęp między sekcjami daje wyłącznie `.page-content` gap. */
  .wartosci {
    padding: 0;
    scroll-margin-top: 96px; /* fixed nav — kotwica #o-mnie */
  }

  /* --- Wprowadzenie --- */
  .w-intro {
    max-width: 680px;
    margin: 0 auto 72px; /* cięższe intro → większy krok do siatki (celowe) */
    display: flex;
    flex-direction: column;
    gap: 32px;
    text-align: center;
    align-items: center;
  }
  /* Mała etykieta 13px/500 — odpowiednik `label-large` */
  .w-label {
    font-size: .8rem;
    font-weight: var(--weight-emphasis);
    letter-spacing: var(--ls-label);
    text-transform: uppercase;
    color: var(--muted);
  }
  /* Duży tekst lead — jak nagłówki (Parkinsans 800) */
  .w-headline {
    /* Dolna granica 1.15rem, nie 1.5rem: na 390px tekst w 24px zajmował
       osiem linii i wypełniał cały ekran przed kafelkami. */
    font-family: var(--font-naglowek);
    font-size: clamp(1.15rem, 2.6vw, 2rem);
    font-weight: var(--weight-display);
    line-height: var(--lh-prose);
    letter-spacing: -0.01em;
    color: var(--ink);
    margin: 0;
  }
  .w-cta {
    margin-top: 0;
    align-self: center;
  }

  /* --- Siatka kafelków --- */
  .w-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 48px 40px;
  }
  .w-karta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    text-align: center;
    /* stan początkowy animacji pojawiania */
    opacity: 0;
    transform: translateY(var(--distance-medium));
    transition: opacity var(--duration-very-slow) var(--ease-in-out),
                transform var(--duration-very-slow) var(--ease-in-out);
  }
  .w-karta.in-view { opacity: 1; transform: none; }

  /* Ikona 64×64 w kolorowym kółku — każdy kafelek w innym pastelu,
     tej samej palety co karty w Ofercie i chipsy. */
  /* Każdy kafelek ma parę: jasny pastel na kółko i ciemny wariant TEGO
     SAMEGO odcienia na rysunek. Ciemny NIE powstaje przez mieszanie z --ink
     (ten jest zielony i ściągał każdą ikonę w szarą zieleń) — to osobno
     dobrane kolory o tym samym odcieniu, niższej jasności. */
  .w-karta:nth-child(1) .w-ikona { --ikona: var(--pastel-limonka); --ikona-ciemna: oklch(0.52 0.11 82);  } /* miód */
  .w-karta:nth-child(2) .w-ikona { --ikona: var(--pastel-szczaw);  --ikona-ciemna: oklch(0.50 0.10 155); } /* mech */
  .w-karta:nth-child(3) .w-ikona { --ikona: var(--pastel-piasek);  --ikona-ciemna: oklch(0.52 0.10 48);  } /* glina */
  .w-karta:nth-child(4) .w-ikona { --ikona: var(--pastel-lipa);    --ikona-ciemna: oklch(0.52 0.08 35);  } /* pyłek */
  .w-ikona {
    width: 64px;
    height: 64px;
    flex-shrink: 0;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--ikona);
    color: var(--ikona-ciemna);
  }
  .w-ikona svg { width: 30px; height: 30px; display: block; }

  .w-tekst { display: flex; flex-direction: column; gap: 8px; }
  .w-tekst h3 { font-size: 1.15rem; font-weight: 700; color: var(--ink); text-wrap: balance; }
  .w-tekst p {
    font-size: var(--text-body);
    line-height: 1.55;
    color: var(--muted);
    /* równe linie w wyśrodkowanym opisie — nie zostawia „schodków” */
    text-wrap: balance;
  }

  @media (prefers-reduced-motion: reduce) {
    .w-karta { transition: none; opacity: 1; transform: none; }
  }

  @media (max-width: 900px) {
    .w-intro { margin-bottom: 48px; gap: 20px; }
    .w-grid { grid-template-columns: repeat(2, 1fr); gap: 48px 28px; }
  }
  @media (max-width: 520px) {
    .w-grid { grid-template-columns: 1fr; gap: 44px; }
  }

  /* --- Modal „O mnie” --- */
  .om-modal {
    position: fixed; inset: 0; z-index: 100;
    display: flex; align-items: center; justify-content: center;
    padding: max(16px, env(safe-area-inset-top))
             max(16px, env(safe-area-inset-right))
             max(16px, env(safe-area-inset-bottom))
             max(16px, env(safe-area-inset-left));
    opacity: 0; pointer-events: none;
    visibility: hidden;
    transition: opacity var(--duration-quick) var(--ease-smooth-out),
                visibility 0s linear var(--duration-quick);
  }
  .om-modal.open {
    opacity: 1; pointer-events: auto; visibility: visible;
    transition: opacity var(--duration-fast) var(--ease-smooth-out),
                visibility 0s;
  }
  .om-modal-backdrop {
    position: absolute; inset: 0;
    background: rgba(20,30,18,.55); backdrop-filter: blur(3px);
  }
  .om-modal-dialog {
    position: relative; z-index: 1;
    background: var(--white); border-radius: var(--radius);
    max-width: 720px; width: 100%;
    max-height: 85vh;
    max-height: 85dvh;
    display: flex; flex-direction: column;
    overflow: hidden;
    box-shadow: 0 30px 80px rgba(0,0,0,.3);
    transform: translateY(var(--distance-medium)) scale(var(--scale-large));
    transition: transform var(--duration-quick) var(--ease-smooth-out);
  }
  .om-modal.open .om-modal-dialog {
    transform: none;
    transition: transform var(--duration-fast) var(--ease-smooth-out);
  }
  @media (prefers-reduced-motion: reduce) {
    .om-modal, .om-modal-dialog { transition: none; }
    .om-modal-dialog { transform: none; }
  }
  .om-modal-scroll {
    overflow-y: auto;
    padding: 40px 40px 24px;
    -webkit-overflow-scrolling: touch;
  }
  .om-modal-foot {
    flex-shrink: 0;
    padding: 32px 40px;
    background: var(--white);
    border-top: 1px solid var(--line);
  }
  .om-modal-foot .btn { margin: 0; width: auto; }
  .om-modal-close {
    position: absolute; top: 24px; inset-inline-end: 24px;
    z-index: 2;
    width: 44px; height: 44px; border-radius: 50%; border: none;
    background: rgba(255, 255, 255, .92);
    box-shadow: 0 2px 12px rgba(0, 0, 0, .18);
    cursor: pointer;
    color: var(--ink);
    display: grid; place-items: center;
    transition: background-color var(--duration-quick) var(--ease-smooth-out),
                transform var(--duration-quick) var(--ease-smooth-out);
  }
  .om-modal-close:active { transform: scale(var(--scale-large)); }
  .om-modal-close:hover { background: var(--white); }

  .om-photo {
    margin: -40px -40px 28px;
    overflow: hidden;
    background: var(--cream);
  }
  .om-photo img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    /* Twarz i wieniec są po prawej — kadr trzyma je w polu widzenia */
    object-position: 78% 22%;
  }

  .om-eyebrow {
    font-size: .8rem;
    font-weight: var(--weight-emphasis);
    letter-spacing: var(--ls-label);
    text-transform: uppercase;
    color: var(--muted);
    margin: 0 0 8px;
  }
  .om-modal-dialog h3 {
    font-family: var(--font-naglowek);
    font-size: 1.6rem;
    font-weight: var(--weight-display);
    margin: 0 0 6px;
    padding-inline-end: 48px;
    color: var(--ink);
  }
  .om-subtitle {
    color: var(--lime-dark);
    font-weight: var(--weight-ui);
    letter-spacing: .01em;
    margin: 0 0 20px;
  }
  .om-lead {
    font-family: var(--font-naglowek);
    font-size: clamp(1.05rem, 2vw, 1.2rem);
    font-weight: var(--weight-display);
    line-height: var(--lh-prose);
    color: var(--ink);
    margin: 0 0 8px;
  }
  .om-h {
    font-size: 1.05rem;
    font-weight: var(--weight-display);
    color: var(--ink);
    margin: 32px 0 12px;
    padding-top: 20px;
    border-top: 3px solid var(--pastel-szczaw);
  }
  .om-p {
    color: var(--muted);
    margin: 0 0 14px;
    line-height: var(--lh-body);
  }
  .om-check {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 10px;
  }
  .om-check li {
    position: relative;
    padding-inline-start: 26px;
    color: var(--muted);
    font-size: var(--text-body);
    line-height: 1.5;
  }
  .om-check li::before {
    content: "";
    position: absolute;
    inset-inline-start: 0; top: .42em;
    width: 14px; height: 8px;
    border-inline-start: 2.5px solid var(--lime-dark);
    border-bottom: 2.5px solid var(--lime-dark);
    transform: rotate(-45deg);
  }

  @media (max-width: 720px) {
    .om-modal-scroll { padding: 24px 20px 16px; }
    .om-modal-foot { padding: 32px 20px; }
    .om-modal-foot .btn { width: 100%; }
    .om-modal-close { top: 16px; inset-inline-end: 16px; }
    .om-modal-dialog h3 { font-size: 1.35rem; }
    .om-photo {
      margin: -24px -20px 20px;
    }
    .om-photo img {
      aspect-ratio: 5 / 4;
      object-position: 82% 18%;
    }
  }
</style>
